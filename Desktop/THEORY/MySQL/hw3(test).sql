/* Для выполнения заданий используется язык запросов SQL. 
Предполагается, что существует таблица staff с колонками: id (идентификатор сотрудника), 
name (имя сотрудника), age (возраст сотрудника), post (специальность сотрудника), 
salary (заработная плата сотрудника). */
CREATE TABLE staff (
  id INT PRIMARY KEY,
  name VARCHAR(50),
  age INT,
  post VARCHAR(50),
  salary DECIMAL(10,2)
);

INSERT INTO staff (id, name, age, post, salary) VALUES
(1, 'Иванов Иван Иванович', 30, 'менеджер', 50000.00),
(2, 'Петров Петр Петрович', 25, 'разработчик', 80000.00),
(3, 'Сидоров Сидор Сидорович', 40, 'бухгалтер', 60000.00),
(4, 'Козлова Екатерина Владимировна', 27, 'администратор', 35000.00),
(5, 'Новиков Дмитрий Андреевич', 33, 'дизайнер', 55000.00),
(6, 'Михайлова Ольга Викторовна', 28, 'тестировщик', 45000.00),
(7, 'Смирнов Андрей Васильевич', 45, 'аналитик', 70000.00),
(8, 'Федорова Анастасия Михайловна', 26, 'разработчик', 75000.00),
(9, 'Козлов Илья Алексеевич', 29, 'менеджер', 60000.00),
(10, 'Григорьев Сергей Васильевич', 35, 'разработчик', 90000.00);

-- Отсортировать данные по полю заработная плата (salary) в порядке убывания:
SELECT * FROM staff ORDER BY salary DESC;
-- Отсортировать данные по полю заработная плата (salary) в порядке возрастания:
SELECT * FROM staff ORDER BY salary ASC;

-- Вывести 5 максимальных заработных плат (salary):
SELECT salary FROM staff ORDER BY salary DESC LIMIT 5;
-- Посчитать суммарную зарплату (salary) по каждой специальности (роst):
SELECT post, SUM(salary) as total_salary 
FROM staff 
GROUP BY post;
-- Найти количество сотрудников с должностью «Рабочий» в возрасте от 24 до 49 лет включительно:
SELECT COUNT(*) as count_workers 
FROM staff 
WHERE post = 'Рабочий' AND age BETWEEN 24 AND 49;
-- Найти количество специальностей:
SELECT COUNT(DISTINCT post) as count_posts 
FROM staff;
-- Вывести специальности, у которых средний возраст сотрудников меньше 30 лет (включительно):
SELECT post 
FROM staff 
GROUP BY post 
HAVING AVG(age) <= 30;